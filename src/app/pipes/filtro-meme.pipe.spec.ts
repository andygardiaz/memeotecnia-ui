import { FiltroMemePipe } from './filtro-meme.pipe';

describe('FiltroMemePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroMemePipe();
    expect(pipe).toBeTruthy();
  });
});
